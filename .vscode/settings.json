// Place your settings in this file to overwrite default and user settings.
{
  "vsicons.presets.angular": true,
  "files.exclude": {
    "**/*.js": {
      "when": "$(basename).ts"
    },
    "**/*.map": {
      "when": "$(basename).map"
    }
  }
}
